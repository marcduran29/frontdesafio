import React from 'react';
import { Paper, List } from '@mui/material';
import { Header, Loading, ErrorMessage, ListItem } from './Helpers';

const BrokerList = ({ onSelectBroker }) => {
    

    return (
        <Paper sx={{ p: 2 }}>
            <Header title="Broker List" />


        </Paper>
    );
};

export default BrokerList;
